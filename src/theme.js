import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette : {
        primary : {
            main: "#c0392b",
            light : "#e74c3c"
        },
        secondary : {
            main : "#B22F59"
        },
        third : {
            main : "#DF9A8C"
        }
    }
})